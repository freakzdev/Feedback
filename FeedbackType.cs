namespace Feedback
{
  // Enum representing the type of Feedback Message
  public enum FeedbackType
  {
    // On error response
    error,
    // On info response
    info,
    // On success response
    success,
    // On warning response
    warning
  }
}